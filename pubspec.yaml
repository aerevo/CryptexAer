name: z_kinetic_pro
description: Military-grade biometric security suite with Firebase Black Box AI
publish_to: 'none'
version: 3.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  uuid: ^4.0.0
  
  # 🔥 FIREBASE CORE (NEW!)
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  cloud_functions: ^4.6.0
  cloud_firestore: ^4.14.0
  
  # Sensors & Device Info
  sensors_plus: ^4.0.0
  device_info_plus: ^9.0.0
  package_info_plus: ^5.0.0
  
  # Security & Crypto
  crypto: ^3.0.3
  flutter_secure_storage: ^9.0.0
  shared_preferences: ^2.2.0
  uuid: ^4.0.0
  
  # Database (Local fallback)
  sqflite: ^2.3.0
  path: ^1.8.3
  
  # HTTP (Legacy - can remove after migration)
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

