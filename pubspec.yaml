name: z_kinetic_secure
description: Z-Kinetic Biometric Lock - Server Authority Mode (SECURE!)
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  http: ^1.1.0
  sensors_plus: ^4.0.2
  safe_device: ^1.1.8
  geolocator: ^11.0.0
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/z_wheel.png
