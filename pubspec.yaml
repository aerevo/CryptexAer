name: cryptex_aer
description: Z-KINETIC Security Suite
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  device_info_plus: ^11.1.0   # Untuk Guard 1 (Check Emulator)
  package_info_plus: ^8.1.0   # Untuk baca version app
  http: ^1.2.0                # Untuk Guard 2 (Call Server)
  # -------------------------------------------

  # Core Security
  crypto: ^3.0.3
  flutter_secure_storage: ^9.0.0
  
  # Device & Sensors
  device_info_plus: ^9.0.0
  sensors_plus: ^3.0.0
  shared_preferences: ^2.2.0
  
  # Networking
  http: ^1.1.0
  
  # ✅ CRITICAL: Add these missing dependencies
  sqflite: ^2.3.0      # For SQL incident storage
  path: ^1.8.3         # For database path
  uuid: ^4.0.0         # For unique device ID

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

