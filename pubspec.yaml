# File: pubspec.yaml
# 🛡️ Z-KINETIC FINAL SURGICAL BUILD
# Status: VIBRATION REMOVED (NATIVE SWITCH) ✅

name: z_kinetic_pro
description: Military-grade biometric security suite
publish_to: 'none'
version: 3.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 🔥 FIREBASE SUITE
  firebase_core: ^2.27.0
  firebase_auth: ^4.16.0
  cloud_functions: ^4.6.0
  cloud_firestore: ^4.15.8
  firebase_remote_config: ^4.3.3
  firebase_app_check: ^0.2.1+17

  # 🔐 UTILITIES
  uuid: ^4.0.0
  crypto: ^3.0.3
  intl: ^0.18.1
  encrypt: ^5.0.3

  # 📱 SENSORS
  sensors_plus: ^4.0.0
  device_info_plus: ^9.0.0
  package_info_plus: ^5.0.0
  flutter_secure_storage: ^9.0.0
  local_auth: ^2.1.6
  http: ^1.1.0

  # ⚠️ PLUGIN "VIBRATION" TELAH DIBUANG DARI SINI
  # Kita guna HapticFeedback (Native)

  # 💾 DATABASE
  sqflite: ^2.3.3+1    # ✅ Versi Fix Registrar
  shared_preferences: ^2.2.2 # ✅ Wajib ada

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
