name: Secure APK Build (Gradle Template Override)
on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build-apk:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v3

      - name: ☕ Setup Java 17
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: 🦅 Setup Flutter (Stable)
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: 🔥 Nuclear Clean
        run: |
          echo "==> Removing ALL Flutter artifacts..."
          rm -rf android/ ios/ build/ .dart_tool/ .flutter-plugins* .packages .metadata pubspec.lock
          echo "✅ Clean complete"

      - name: 💾 Backup Essential Files
        run: |
          echo "==> Backing up lib, pubspec.yaml, server, assets..."
          mkdir -p /tmp/cryptex_backup
          cp -r lib /tmp/cryptex_backup/
          cp pubspec.yaml /tmp/cryptex_backup/
          [ -d "server" ] && cp -r server /tmp/cryptex_backup/
          [ -d "assets" ] && cp -r assets /tmp/cryptex_backup/
          echo "✅ Backup complete"

      - name: 🏗️ Create Fresh Project
        run: |
          echo "==> Creating fresh Flutter project..."
          cd ..
          flutter create --platforms android --org com.cryptex --project-name cryptex_aer fresh_build
          
          test -d fresh_build/android || { echo "ERROR: Fresh project failed!"; exit 1; }
          echo "✅ Fresh project created"

      - name: 📦 Restore User Code
        run: |
          echo "==> Restoring backed up files..."
          cd ../fresh_build
          rm -rf lib/
          cp -r /tmp/cryptex_backup/lib .
          cp /tmp/cryptex_backup/pubspec.yaml .
          [ -d "/tmp/cryptex_backup/server" ] && cp -r /tmp/cryptex_backup/server .
          [ -d "/tmp/cryptex_backup/assets" ] && cp -r /tmp/cryptex_backup/assets .
          echo "✅ Code restored"

      - name: 🔧 FORCE FIX: Override build.gradle (Critical)
        run: |
          cd ../fresh_build
          echo "==> Overriding android/app/build.gradle with modern template..."
          
          cat > android/app/build.gradle << 'BUILD_GRADLE_EOF'
          plugins {
              id "com.android.application"
              id "kotlin-android"
              id "dev.flutter.flutter-gradle-plugin"
          }
          
          android {
              namespace = "com.cryptex.cryptex_aer"
              compileSdk = flutter.compileSdkVersion
              ndkVersion = flutter.ndkVersion
          
              compileOptions {
                  sourceCompatibility = JavaVersion.VERSION_17
                  targetCompatibility = JavaVersion.VERSION_17
              }
          
              kotlinOptions {
                  jvmTarget = "17"
              }
          
              defaultConfig {
                  applicationId = "com.cryptex.cryptex_aer"
                  minSdk = flutter.minSdkVersion
                  targetSdk = flutter.targetSdkVersion
                  versionCode = flutter.versionCode
                  versionName = flutter.versionName
              }
          
              buildTypes {
                  release {
                      signingConfig = signingConfigs.debug
                  }
              }
          }
          
          flutter {
              source = "../.."
          }
          BUILD_GRADLE_EOF
          
          echo "✅ build.gradle overridden with modern template"

      - name: 🔧 Fix settings.gradle
        run: |
          cd ../fresh_build
          echo "==> Ensuring settings.gradle is correct..."
          
          cat > android/settings.gradle << 'SETTINGS_GRADLE_EOF'
          pluginManagement {
              def flutterSdkPath = {
                  def properties = new Properties()
                  file("local.properties").withInputStream { properties.load(it) }
                  def flutterSdkPath = properties.getProperty("flutter.sdk")
                  assert flutterSdkPath != null, "flutter.sdk not set in local.properties"
                  return flutterSdkPath
              }()
          
              includeBuild("$flutterSdkPath/packages/flutter_tools/gradle")
          
              repositories {
                  google()
                  mavenCentral()
                  gradlePluginPortal()
              }
          }
          
          plugins {
              id "dev.flutter.flutter-plugin-loader" version "1.0.0"
              id "com.android.application" version "8.1.0" apply false
              id "org.jetbrains.kotlin.android" version "1.9.0" apply false
          }
          
          include ":app"
          SETTINGS_GRADLE_EOF
          
          echo "✅ settings.gradle configured"

      - name: 🔧 Fix gradle.properties
        run: |
          cd ../fresh_build
          echo "==> Configuring gradle.properties..."
          
          cat > android/gradle.properties << 'GRADLE_PROPS_EOF'
          org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G -XX:+HeapDumpOnOutOfMemoryError
          android.useAndroidX=true
          android.enableJetifier=true
          android.defaults.buildfeatures.buildconfig=true
          android.nonTransitiveRClass=false
          android.nonFinalResIds=false
          GRADLE_PROPS_EOF
          
          echo "✅ gradle.properties configured"

      - name: 🔐 Inject MainActivity.kt
        run: |
          cd ../fresh_build
          echo "==> Creating MainActivity.kt..."
          
          MAIN_DIR="android/app/src/main/kotlin/com/cryptex/cryptex_aer"
          mkdir -p "$MAIN_DIR"
          
          cat > "$MAIN_DIR/MainActivity.kt" << 'KOTLIN_EOF'
          package com.cryptex.cryptex_aer
          
          import android.os.Bundle
          import android.view.WindowManager
          import io.flutter.embedding.android.FlutterActivity
          import io.flutter.embedding.engine.FlutterEngine
          import io.flutter.plugin.common.MethodChannel
          import java.io.File
          import android.content.pm.PackageManager
          
          class MainActivity: FlutterActivity() {
              private val SECURITY_CHANNEL = "com.cryptex/security"
              
              override fun onCreate(savedInstanceState: Bundle?) {
                  super.onCreate(savedInstanceState)
                  window.setFlags(
                      WindowManager.LayoutParams.FLAG_SECURE,
                      WindowManager.LayoutParams.FLAG_SECURE
                  )
              }
              
              override fun configureFlutterEngine(flutterEngine: FlutterEngine) {
                  super.configureFlutterEngine(flutterEngine)
                  
                  MethodChannel(flutterEngine.dartExecutor.binaryMessenger, SECURITY_CHANNEL)
                      .setMethodCallHandler { call, result ->
                          when (call.method) {
                              "enableScreenshotProtection" -> {
                                  try {
                                      window.addFlags(WindowManager.LayoutParams.FLAG_SECURE)
                                      result.success(true)
                                  } catch (e: Exception) {
                                      result.error("PROTECTION_ERROR", e.message, null)
                                  }
                              }
                              "checkScreenshot" -> result.success(false)
                              "detectRootBypass" -> {
                                  try {
                                      val packages = call.argument<List<String>>("packages") ?: emptyList()
                                      val paths = call.argument<List<String>>("paths") ?: emptyList()
                                      result.success(checkRootBypass(packages, paths))
                                  } catch (e: Exception) {
                                      result.error("ROOT_CHECK_ERROR", e.message, null)
                                  }
                              }
                              else -> result.notImplemented()
                          }
                      }
              }
              
              private fun checkRootBypass(packages: List<String>, paths: List<String>): Boolean {
                  paths.forEach { if (File(it).exists()) return true }
                  packages.forEach { if (isPackageInstalled(it, packageManager)) return true }
                  return false
              }
              
              private fun isPackageInstalled(pkg: String, pm: PackageManager): Boolean {
                  return try { pm.getPackageInfo(pkg, 0); true } catch (e: Exception) { false }
              }
          }
          KOTLIN_EOF
          
          echo "✅ MainActivity.kt created"

      - name: ⚙️ Configure AndroidManifest.xml
        run: |
          cd ../fresh_build
          echo "==> Adding permissions to AndroidManifest.xml..."
          
          MANIFEST="android/app/src/main/AndroidManifest.xml"
          sed -i '/<application/i \    <uses-permission android:name="android.permission.VIBRATE"/>' "$MANIFEST"
          
          echo "✅ AndroidManifest.xml configured"

      - name: 📦 Install Dependencies
        run: |
          cd ../fresh_build
          echo "==> Running flutter pub get..."
          flutter pub get
          
          echo "==> Verifying dependencies..."
          flutter pub deps --style=compact | grep -E "flutter_jailbreak|flutter_secure_storage|sensors_plus" || true

      - name: 🔍 Patch Jailbreak Detection Library
        run: |
          echo "==> Patching flutter_jailbreak_detection..."
          
          JB_GRADLE=$(find ~/.pub-cache/hosted/pub.dev -path "*/flutter_jailbreak_detection-*/android/build.gradle" 2>/dev/null | head -n 1)
          
          if [ -n "$JB_GRADLE" ]; then
            echo "Found: $JB_GRADLE"
            
            if ! grep -q "namespace" "$JB_GRADLE"; then
              sed -i '/android {/a \    namespace = "flutter.jailbreak_detection"' "$JB_GRADLE"
            fi
            
            if ! grep -q "jvmTarget" "$JB_GRADLE"; then
              cat >> "$JB_GRADLE" << 'PATCH_EOF'
          
          android {
              compileOptions {
                  sourceCompatibility = JavaVersion.VERSION_17
                  targetCompatibility = JavaVersion.VERSION_17
              }
          }
          
          tasks.withType(org.jetbrains.kotlin.gradle.tasks.KotlinCompile).configureEach {
              kotlinOptions {
                  jvmTarget = "17"
              }
          }
          PATCH_EOF
            fi
            
            echo "✅ Library patched"
          else
            echo "⚠️ Library not found (may be cached)"
          fi

      - name: 🧪 Verify Gradle Files
        run: |
          cd ../fresh_build
          echo "==> Verifying critical Gradle files..."
          
          echo "Checking build.gradle..."
          grep -q "namespace = \"com.cryptex.cryptex_aer\"" android/app/build.gradle && echo "✓ Namespace found"
          grep -q "dev.flutter.flutter-gradle-plugin" android/app/build.gradle && echo "✓ Flutter plugin found"
          
          echo "Checking settings.gradle..."
          grep -q "dev.flutter.flutter-plugin-loader" android/settings.gradle && echo "✓ Plugin loader found"
          
          echo "Checking MainActivity.kt..."
          test -f android/app/src/main/kotlin/com/cryptex/cryptex_aer/MainActivity.kt && echo "✓ MainActivity.kt exists"
          
          echo ""
          echo "Running Flutter doctor..."
          flutter doctor -v
          
          echo "✅ All validations passed"

      - name: 🚀 Build Release APK
        run: |
          cd ../fresh_build
          echo "==> Building release APK with obfuscation..."
          
          flutter build apk --release --obfuscate --split-debug-info=./debug_symbols
          
          if [ ! -f "build/app/outputs/flutter-apk/app-release.apk" ]; then
            echo "❌ APK build failed!"
            echo "Checking build directory..."
            find build/app/outputs -type f 2>/dev/null || echo "No outputs found"
            exit 1
          fi
          
          APK_SIZE=$(du -h build/app/outputs/flutter-apk/app-release.apk | cut -f1)
          echo ""
          echo "✅ BUILD SUCCESSFUL!"
          echo "📦 APK Size: $APK_SIZE"

      - name: 📤 Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: cryptex-aer-release
          path: ../fresh_build/build/app/outputs/flutter-apk/app-release.apk
          retention-days: 30

      - name: 📤 Upload Debug Symbols
        uses: actions/upload-artifact@v4
        with:
          name: debug-symbols
          path: ../fresh_build/debug_symbols/
          retention-days: 90
        continue-on-error: true

      - name: 🎉 Success Summary
        run: |
          cd ../fresh_build
          echo ""
          echo "╔════════════════════════════════════════════════╗"
          echo "║     ✅ CRYPTEX AER BUILD SUCCESSFUL ✅         ║"
          echo "╠════════════════════════════════════════════════╣"
          echo "║ Package: com.cryptex.cryptex_aer              ║"
          echo "║ APK: $(du -h build/app/outputs/flutter-apk/app-release.apk | cut -f1 | awk '{printf "%-38s", $1}')║"
          echo "║ Obfuscation: ENABLED                          ║"
          echo "║ Security: Screenshot + Root Detection         ║"
          echo "╚════════════════════════════════════════════════╝"
